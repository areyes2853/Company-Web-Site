<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
</head>
<%- include('./nav') %>
<body>
    <h1>Hi!</h1>
    
    <ul>
        <% if (work && work.length > 0) { %>
            <% work.forEach(function(past) { %>
                <li><%= past.Role %> : <%= past.Work %></li>
            <% }); %>
        <% } else { %>
            <li>No past work available.</li>
        <% } %>
    </ul>
</body>
</html>